%article.biz_category  
  %header    
    %h1= link_to @category.name
  .biz.row    
    - @category.businesses.each do |business|
      .biz.col-sm-2
        = link_to "", nil do
          %p= business.name
